<div class="container">
  <h1>Assign a Bookmark</h1>
  <%= simple_form_for [@bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.association :list %>
    <%= f.input :comment %>
    <%= f.input :photo, as: :file %>
    <%= f.button :submit, class: "btn btn-primary"  %>
  <% end %>
</div>
